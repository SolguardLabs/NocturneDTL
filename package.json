{
  "name": "nocturne-dtl",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tests JavaScript para el nucleo Rust de privacidad contable Nocturne DTL.",
  "scripts": {
    "test": "node --test --test-concurrency=1 tests/node/*.test.js",
    "test:rust": "cargo test --locked",
    "build": "cargo build --locked",
    "loc": "node scripts/check-loc.mjs",
    "test:all": "bash scripts/ci.sh"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
